const evenFibSequence = n => {
  let a = 1, b = 2, c = sum = 0;
  // const filt = fibSeq.filter(e => e <= n && e % 2 === 0).reduce((acc, cur) => acc + cur);
  return c;
}

console.log(evenFibSequence(100));